- Apply transfer READ rules and stable primary-key pagination to public-profile import/export lists (`config/graphql/user_types.py::_filter_visible_transfers`, one helper behind `userexportSet` / `lockedUserexportObjects` / `userimportSet` / `lockedUserimportObjects`). `UserExport` honours guardian READ grants; `UserImport` has no guardian tables, so only creator and public imports are listed.
- Bind moderation tools to their factory actor: `moderator_id` joins `TOOL_ACTOR_IDENTITY_PARAMS` (`opencontractserver/constants/tools.py`), the named set `llms/tools/tool_factory.py::build_inject_params_for_context` fills from the factory `user_id` and hides from the LLM.
- Restrict approval/rejection to messages in the agent's conversation (`llms/agents/pydantic_ai_agents.py::resume_with_approval`). Ephemeral sessions (`persist=False` sub-agents, anonymous chat) have no persisted conversation and now fail with an explicit `ValueError` instead of resolving a synthetic placeholder id against the global `ChatMessage` table; see `docs/architecture/llms/README.md` (Tool Approval) for why this path was never functional.
- `BaseService.filter_visible_qs` (`opencontractserver/shared/services/base.py`) now runs its fail-closed guard on the normalised queryset, so a bare-`BaseVisibilityManager` related manager raises the documented `TypeError` rather than an `AttributeError`.
