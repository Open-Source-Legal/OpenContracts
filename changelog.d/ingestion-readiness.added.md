- Add authorized document and corpus-page search readiness, configuration-aware embedding coverage, bounded targeted repair with persisted results, and remote-worker `verify --readiness`. Corpus copies now inherit completed processing state. Document status observes the newest current path when a document lives in several corpora, or the corpus named by `?corpus=<id>`, and reports the observed `corpus_id`.
