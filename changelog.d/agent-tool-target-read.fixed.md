- Check READ on separately selected document, corpus, annotation, note, thread, and message targets before invoking agent tools (`llms/tools/pydantic_ai_tools.py`). Reuse the current actor and avoid repeating READ for the bound document/corpus. Require existing CRUD on selected document/corpus/note/annotation content-write targets and READ before thread posting; keep malformed-target failures inside normal tool error handling. Preserve service-owned corpus/group selection and file-management permission rules.
