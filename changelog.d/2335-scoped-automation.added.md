- Add hashed, expiring automation credentials with a metadata-only CLI lifecycle (`automation_credential`) and shared GraphQL/import REST authentication. Explicit operation scopes intersect with principal permissions and corpus restrictions; GraphQL checks the selected operation before execution, and chunked uploads preserve credential ownership through rotation while rechecking authorization at every stage. Apply users migration `0032` and document_imports migration `0002` before deploying (#2335).
