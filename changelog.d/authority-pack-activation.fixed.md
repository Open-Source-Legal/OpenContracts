Authority packs now persist versioned artifacts in application storage and atomically activate them with their installed content. Running web and worker processes refresh provider, taxonomy and source-host discovery automatically. API and command installs require authority-administrator permissions and approved charters for publication; failed activation preserves the prior version and exposes its error.
