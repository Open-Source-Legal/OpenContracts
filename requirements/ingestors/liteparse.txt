# LiteParse local document parsing (PDFium-based, optional Tesseract OCR)
# https://github.com/run-llama/liteparse
#
# Pinned to the 2.x series: LiteParseParser is written against the 2.x Python
# API (LiteParse / ParseResult.pages / ParsedPage.text_items / TextItem fields).
# The library is pre-1.0-stable in spirit and has already shipped breaking major
# bumps (0.x -> 1.x -> 2.x), so we cap below the next major to avoid an
# `--upgrade` silently pulling an incompatible 3.x.
liteparse>=2.2.1,<3.0.0
